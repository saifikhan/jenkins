pipeline {
    agent any 
    stages {
        stage('run') {
            steps {
                echo 'python script execution BEGIN'
                echo 'python script execution END'
            }
        }
    }
}
